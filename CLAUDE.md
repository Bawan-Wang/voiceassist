
# CLAUDE.md

This file is a compatibility shim.

For this repository:
- `AGENTS.md` is the workflow authority.
- `.docs/rules.md` is the single source of truth for behavioral guidance,
  coding conventions, and known deviations from those conventions.

If the two documents conflict with already-tracked code, prefer the local file
style unless the task is explicitly a cleanup or refactor.
